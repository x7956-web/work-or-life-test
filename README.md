# 你该换工作，还是换活法？

CloudBase 静态托管版，纯前端 H5 测试页。

## CloudBase 当前配置

- 目标目录：`./`
- 安装命令：`npm install`
- 构建命令：`npm run build`
- 构建产物目录：`./dist`
- 部署路径：`/`

`npm run build` 会把 `tcb-deploy/public` 复制到 `dist`，不依赖 Vite、Sharp、后端服务或数据库。

## 测试入口

```text
/#/test/work-or-life
```
